import ajaxCity from "./ajaxCity";

const onChange=()=>{
    const input=document.querySelector('input');
    input.addEventListener("change", updateValue);
    console.log=target.value;
}

export default onChange